# 서버의 infra/docker/.env.prod 로 복사한다 (커밋 금지). deploy.sh 가 없으면 자동 생성한다.
DB_NAME=saju
DB_USERNAME=saju
# required: 서버에서 생성한 무작위 값
DB_PASSWORD=
CORS_ORIGINS='["https://saju.marinkim.xyz"]'
LOG_LEVEL=info
